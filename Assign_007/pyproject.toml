[project]
name = "assign-007"
version = "0.1.0"
description = "ERA V5 Assignment 07 — Fourier alternative to Kronecker embeddings (Problem #4)"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "torch>=2.2.0",
    "numpy>=1.26.0",
    "matplotlib>=3.8.0",
    "pytest>=8.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
