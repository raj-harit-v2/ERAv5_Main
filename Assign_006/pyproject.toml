[project]
name = "assign-006"
version = "0.1.0"
description = "ERA V5 Assignment 06 — Training Data Execution System"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "torch>=2.2.0",
    "numpy>=1.26.0",
    "transformers>=4.40.0",
    "pytest>=8.0.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
